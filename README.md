# CI/CD Deployment of React Application on AWS EC2 using GitHub Actions

## Live Website

http://13.203.154.124

The application is hosted on an AWS EC2 Ubuntu instance and served using the Nginx web server.

## Project Overview

This project demonstrates a complete CI/CD pipeline for automatically building and deploying a React application to an AWS EC2 server using GitHub Actions.

The goal of the project is to automate the deployment process so that every code change pushed to GitHub is automatically built and deployed to a live server without manual intervention.

## Technologies Used

Git and GitHub for version control
GitHub Actions for CI/CD automation
AWS EC2 (Ubuntu) as the hosting server
Nginx as the web server
Node.js and npm for application build process
React and Vite for frontend development
Linux for server management

## Architecture

Developer pushes code → GitHub repository → GitHub Actions pipeline → SSH connection → AWS EC2 server → Nginx web server → Live website

## Project Structure

.
├── src/
├── public/
├── dist/
├── .github/workflows/
│   └── deploy.yml
├── package.json
└── README.md

src contains the React application source code.

dist contains the production build generated by the build process.

.github/workflows contains the CI/CD pipeline configuration.

package.json manages project dependencies and scripts.

## Server Setup

An AWS EC2 Ubuntu instance was launched to host the application.

The following configuration steps were completed:

Install Node.js and npm
Install and configure Nginx
Clone the GitHub repository
Build the application
Deploy the build files to the Nginx root directory

Deployment directory used:

/var/www/html/

Nginx serves the static website from this directory.

## Application Build Process

The React application uses a production build process to generate optimized static files.

Commands used:

npm install
npm run build

The build command creates a dist folder containing optimized HTML, CSS, and JavaScript files that are deployed to the server.

## CI/CD Pipeline

The CI/CD pipeline is implemented using GitHub Actions.

Workflow file location:

.github/workflows/deploy.yml

The pipeline automatically runs whenever code is pushed to the main branch.

### Deployment Process

1. Developer pushes code to GitHub
2. GitHub Actions workflow starts automatically
3. Workflow connects to EC2 using SSH
4. Latest code is pulled from the repository
5. Project dependencies are installed
6. Application build process runs
7. Existing deployment files are removed
8. New build files are copied to the Nginx directory
9. Nginx service restarts
10. Updated website is available on the live server

## Secure Authentication

Secure server access from GitHub Actions to the EC2 instance was configured using SSH keys.

Steps performed:

Generate SSH key pair
Store the private key in GitHub Secrets
Configure the workflow to use the key during deployment

Secret used:

EC2_SSH_KEY

## Automated Deployment

After CI/CD setup, every code push automatically updates the live application.

Developer updates code
Code is pushed to GitHub
GitHub Actions runs the deployment workflow
EC2 pulls the latest code
Application is rebuilt
New version is deployed automatically

## Running the Project Locally

Clone the repository

git clone https://github.com/your-username/your-repository

Navigate to the project folder

cd project-folder

Install dependencies

npm install

Run development server

npm run dev

Build the project

npm run build

## Skills Demonstrated

Linux server administration
Cloud infrastructure management using AWS EC2
Nginx web server configuration
Git version control
CI/CD pipeline implementation using GitHub Actions
Secure SSH authentication
Automated application deployment

## Future Improvements

Add a custom domain to the website instead of accessing it through the EC2 public IP address.

Configure DNS records to point the domain to the EC2 server.

Implement HTTPS using SSL certificates.

Potential future improvements include:

Docker containerization
Load balancing
Infrastructure automation using Terraform
Monitoring and logging integration

